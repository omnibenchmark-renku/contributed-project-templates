- description: A simple Renku project with a basic directory structure to add a dataset
    to an omnibenchmark project.
  folder: omni-data-py
  icon: img/dataset.png
  name: Omnibenchmark dataset
  variables:
    benchmark_name:
      default_value: omni_batch_py
      description: Name of the omnibenchmark to link this project with.
      enum:
      - omni_batch_py
      - omniclustering
      - omniclustering_pinned
      - semantic_iris
      - iris_example
      - CCC_benchmark
      - iris_versioned
      - omni_clustering
      - bc2_2023
      - terraform
      - spatial-clustering
      type: enum
    dataset_keyword:
      description: Dataset keyword to make this dataset reachable for other projects/
        omnibenchmarks components.
    metadata_description:
      description: (optional) A complete description of the experimental design, for
        e.g. the treatment, condition, specificities, etc.
    project_title:
      description: Long, human readable project title.
    study_link:
      description: (optional) If any, link to the original study (zenodo, GEO,...).
    study_note:
      description: (optional) Any additional notes about the dataset.
    study_tissue:
      description: (optional) Tissue/ cell line used to generate the data.
- description: A simple renku project with a basic directory structure to add a data
    processing to an omnibenchmark project.
  folder: omni-processed-py
  icon: img/processing.png
  name: Omnibenchmark processing
  variables:
    benchmark_name:
      defaul_value: omni_batch_py
      description: Name of the omnibenchmark to link this project with.
      enum:
      - omni_batch_py
      - omniclustering
      - omniclustering_pinned
      - semantic_iris
      - iris_example
      - CCC_benchmark
      - iris_versioned
      - omni_clustering
      - bc2_2023
      - terraform
      - spatial-clustering
      type: enum
    processed_keyword:
      description: Processed data keyword to make this dataset reachable for other
        projects/benchmark components.
    project_title:
      description: Long, human readable project title.
- description: A simple renku project with a basic directory structure to add a method
    to an omnibenchmark project.
  folder: omni-method-py
  icon: img/method.png
  name: Omnibenchmark method
  variables:
    benchmark_name:
      default_value: omni_batch_py
      description: Name of the omnibenchmark to link this project with.
      enum:
      - omni_batch_py
      - omniclustering
      - omniclustering_pinned
      - semantic_iris
      - iris_example
      - CCC_benchmark
      - iris_versioned
      - omni_clustering
      - bc2_2023
      - terraform
      - spatial-clustering
      type: enum
    method_keyword:
      description: Method keyword to make this dataset reachable for other projects/benchmark
        components.
    project_title:
      description: Long, human readable project title.
- description: A simple renku project with a basic directory structure to add a method
    to an omnibenchmark project.
  folder: omni-param-py
  icon: img/parameter.png
  name: Omnibenchmark parameter space
  variables:
    benchmark_name:
      default_value: omni_batch_py
      description: Name of the omnibenchmark to link this project with.
      enum:
      - omni_batch_py
      - omniclustering
      - omniclustering_pinned
      - semantic_iris
      - iris_example
      - CCC_benchmark
      - iris_versioned
      - omni_clustering
      - bc2_2023
      - terraform
      - spatial-clustering
      type: enum
    parameter_keyword:
      description: Parameter keyword to make this dataset reachable for other projects/benchmark
        components.
    project_title:
      description: Long, human readable project title.
- description: A simple renku project with a basic directory structure to add a metric
    to an omnibenchmark project.
  folder: omni-metric-py
  icon: img/metric.png
  name: Omnibenchmark metric
  variables:
    benchmark_name:
      default_value: omni_batch_py
      description: Name of the omnibenchmark to link this project with.
      enum:
      - omni_batch_py
      - omniclustering
      - omniclustering_pinned
      - semantic_iris
      - iris_example
      - CCC_benchmark
      - iris_versioned
      - omni_clustering
      - bc2_2023
      - terraform
      - spatial-clustering
      type: enum
    metric_keyword:
      description: Metric keyword to make this dataset reachable for other projects/benchmark
        components.
    project_title:
      description: Long, human readable project title.
